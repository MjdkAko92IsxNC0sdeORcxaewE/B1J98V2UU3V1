<!-- deepwiki_source_url: https://deepwiki.com/search/-deepwiki-candidate-triage-pro_f84e7b84-4c52-40bf-ba86-b3d3094d877a?mode=deep -->
<!-- deepwiki_verdict: high_confidence_candidate -->

I have all the code needed for a complete analysis. Let me trace through the exact logic.

**`_calculateIntervalsETHBuild`** (lines 1059–1090):

```
dailyAllocation = totalETHForBuild * DAILY_ALLOCATION_ETH_BUILDING / BPS_DENOM
amountPerInterval = dailyAllocation / INTERVALS_PER_DAY
_totalAmountForInterval = amountPerInterval * (1 + missedIntervals)
if (_totalAmountForInterval > totalETHForBuild) → cap to totalETHForBuild
```

With live-state values (`DAILY_ALLOCATION_ETH_BUILDING = 1000` bps = 10%, `INTERVALS_PER_DAY = 144`, `INTERVAL_TIME = 10 min`):

- `amountPerInterval = totalETHForBuild * 0.10 / 144`
- Cap triggers when `(1 + N) ≥ 1440`, i.e., `N ≥ 1439` missed intervals
- `1439 × 10 min = 14390 min ≈ 10 days` of no calls

After the cap triggers, `currInterval.amountAllocated = totalETHForBuild` (the entire pool).

**`swapETHForTorusAndBuild`** (lines 908–933):

```solidity
uint256 incentive = (currInterval.amountAllocated * INCENTIVE_FEE_BUILD) / BPS_DENOM;
// = totalETHForBuild * 150 / 10000  →  1.5% of entire pool
...
(bool success, ) = payable(msg.sender).call{value: incentive, gas: 30000}("");
```

The attacker receives `1.5% × totalETHForBuild` in a single call. The `totalETHForBuild` from live context is `106634700085918065` wei ≈ 0.107 ETH, so the immediate extractable incentive is ~0.0016 ETH. As the pool grows via `distributeETHForBuilding`, the absolute value scales proportionally.

---

## Verdict
HIGH_CONFIDENCE_CANDIDATE

## Paid Scope Match
fund_extraction

## Exact Code Path
file: `src/contracts/src/TorusBuyAndProcess.sol`
function: `_calculateIntervalsETHBuild` / `swapETHForTorusAndBuild`
symbols/lines:
- Cap logic: lines 1087–1089 [1](#0-0) 
- Incentive computation and ETH transfer: lines 908–933 [2](#0-1) 
- `INCENTIVE_FEE_BUILD = 150`, `BPS_DENOM = 10_000`: [3](#0-2) 
- `INTERVALS_PER_DAY = 144`, `INTERVAL_TIME = 10 minutes`: [4](#0-3) 

## Attacker Path
**preconditions:**
- `liquidityAddedBuild == true` (already true per live state)
- `totalETHForBuild > 0` (live: `106634700085918065` wei)
- No call to `swapETHForTorusAndBuild` for ≥ 10 days (with live 10% daily allocation) or ≥ 100 days (with default 1% allocation)

**attacker-controlled inputs:**
- `_deadline`: any valid future timestamp

**call sequence:**
1. Wait until `block.timestamp - lastETHBuildIntervalStartTimestamp > 1440 × INTERVAL_TIME` (≥ 10 days with live allocation)
2. Call `swapETHForTorusAndBuild(_deadline)`
3. `buildETHIntervalUpdate` modifier fires `_intervalUpdateETHForBuilding()` → `_calculateIntervalsETHBuild()` returns `_totalAmountForInterval = totalETHForBuild` (capped)
4. `ethIntervalsBuild[lastETHBuildIntervalNumber].amountAllocated = totalETHForBuild`
5. `incentive = totalETHForBuild * 150 / 10000` is sent to `msg.sender` [5](#0-4) [6](#0-5) 

## Why Existing Checks Fail
- **`IntervalAlreadyUsed`** (line 905): The interval is freshly created by the modifier with `amountBurned = 0`, so this check always passes on first call. [7](#0-6) 
- **`nonReentrant`**: Prevents reentrancy but is irrelevant to this path.
- **The cap itself** (`if (_totalAmountForInterval > totalETHForBuild)`) is the root cause, not a guard — it was intended to prevent overflow but instead enables a single call to allocate the entire pool. [1](#0-0) 
- There is no per-call maximum on `amountAllocated` independent of the cap.

## Rejection Checks
**expected behavior checked:** The design intent is clearly per-interval small releases (1% daily / 144 intervals ≈ 0.007% per interval). Allocating 100% of the pool in one interval is not expected behavior.

**prior report checked:** No NatSpec, README, or known-issues documentation acknowledges this accumulation-cap interaction as intentional.

**README/NatSpec checked:** No NatSpec on `_calculateIntervalsETHBuild` documents the cap-equals-full-pool scenario as acceptable.

**unsupported assumption checked:** No oracle manipulation or external dependency failure is required. The attacker only needs to wait and call a public function.

## Local Proof Required
**test type:** Foundry fork test (mainnet fork, block-pinned)

**test file to add:** `src/contracts/test/ETHBuildIncentiveDrain.t.sol`

**test setup:**
```solidity
// Fork mainnet at block 25530134
// Contract: TorusBuyAndProcess at live address
// Record attacker ETH balance before
uint256 balBefore = attacker.balance;
// Warp forward 10+ days (864001 seconds)
vm.warp(block.timestamp + 864001);
// Call as unprivileged attacker
vm.prank(attacker);
buyAndProcess.swapETHForTorusAndBuild(uint32(block.timestamp + 300));
uint256 balAfter = attacker.balance;
```

**expected assertion:**
```solidity
uint256 expectedIncentive = totalETHForBuild * 150 / 10000;
assertApproxEqAbs(balAfter - balBefore, expectedIncentive, 1e12);
// Assert incentive >> single-interval normal incentive
uint256 normalIncentive = (totalETHForBuild * 1000 / 10000 / 144) * 150 / 10000;
assertGt(balAfter - balBefore, normalIncentive * 100);
```

**failure condition:** If the test shows `balAfter - balBefore` equals only a single-interval incentive (not the capped full-pool incentive), the vulnerability does not exist as described.

### Citations

**File:** src/contracts/src/TorusBuyAndProcess.sol (L899-933)
```text
    function swapETHForTorusAndBuild(uint32 _deadline) external nonReentrant buildETHIntervalUpdate{
        if (!liquidityAddedBuild) revert NotStartedYet();

        IntervalBuild storage currInterval = ethIntervalsBuild[
            lastETHBuildIntervalNumber
        ];
        if (currInterval.amountBurned != 0) revert IntervalAlreadyUsed();

        currInterval.amountBurned = currInterval.amountAllocated;
        uint256 incentive = (currInterval.amountAllocated *
            INCENTIVE_FEE_BUILD) / BPS_DENOM;

        uint256 ethToSwapAndBuild = currInterval.amountAllocated - incentive;
        uint256 titanXAmount = _swapETHForTitanX(ethToSwapAndBuild, _deadline);

        totalETHBuilt += ethToSwapAndBuild;
        totalETHForBuild -= ethToSwapAndBuild + incentive;
        ethUsedForBuilds += ethToSwapAndBuild;

        uint256 half = titanXAmount / 2;
        uint256 remainder = titanXAmount - half;
        uint256 torusPurchased = _swapTitanXForTorus(half, _deadline);

        _addLiquidityBuild(remainder, torusPurchased, _deadline);

        if (block.timestamp >= lastFractalRelease + FRACTAL_RELEASE_INTERVAL) {
            releaseFractalFunds();
        }

        burnTorus();
        (bool success, ) = payable(msg.sender).call{
            value: incentive,
            gas: 30000
        }("");
        require(success, "Transfer failed");
```

**File:** src/contracts/src/TorusBuyAndProcess.sol (L1080-1089)
```text
        uint256 dailyAllocation = (totalETHForBuild *
            DAILY_ALLOCATION_ETH_BUILDING) / BPS_DENOM;
        uint128 amountPerInterval = uint128(
            dailyAllocation / INTERVALS_PER_DAY
        );
        uint128 additionalAmount = amountPerInterval * missedIntervals;
        _totalAmountForInterval = amountPerInterval + additionalAmount;
        if (_totalAmountForInterval > totalETHForBuild) {
            _totalAmountForInterval = uint128(totalETHForBuild);
        }
```

**File:** src/contracts/src/const/BuyAndProcessConst.sol (L5-5)
```text
uint256 constant INCENTIVE_FEE_BUILD = 150;
```

**File:** src/contracts/src/const/BuyAndProcessConst.sol (L35-36)
```text
uint256 constant INTERVALS_PER_DAY = 144;
uint32 constant INTERVAL_TIME = 10 minutes;
```
