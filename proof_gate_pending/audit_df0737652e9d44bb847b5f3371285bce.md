<!-- deepwiki_source_url: https://deepwiki.com/search/-deepwiki-candidate-triage-pro_c3d0522c-8f31-41d3-83f3-b9a747c7135e?mode=deep -->
<!-- deepwiki_verdict: needs_local_proof -->

I have all the code I need. Let me analyze the full call chain precisely.

**Key code path:**

`swapTitanXForTorusAndBuild` → `_swapTitanXForTorus(half, _deadline)` → `getTorusQuoteForTitanX(amountTitanX)` → `amountOutMinimum = quote * (100 - titanXToTorusSlippage) / 100`

The critical sections:

`getTorusQuoteForTitanX` at [1](#0-0) :
- Targets `secondsAgo = 15 * 60` (15 minutes)
- **Falls back**: `if (oldestObservation < secondsAgo) secondsAgo = oldestObservation;` — if the pool has low observation cardinality, the TWAP window collapses to near-spot

`_swapTitanXForTorus` at [2](#0-1) :
- `adjustedTorusAmount = (expectedTorusAmount * (100 - titanXToTorusSlippage)) / 100`
- `titanXToTorusSlippage` defaults to 10 (10%), owner-settable 2–100 [3](#0-2) 

`swapTitanXForTorusAndBuild` at [4](#0-3) :
- Caller receives `INCENTIVE_FEE_BUILD = 150` BPS (1.5%) regardless of swap outcome [5](#0-4) 

---

## Verdict
NEEDS_LOCAL_PROOF

## Paid Scope Match
fund_extraction

## Exact Code Path
file: `src/contracts/src/TorusBuyAndProcess.sol`
function: `getTorusQuoteForTitanX`, `_swapTitanXForTorus`, `swapTitanXForTorusAndBuild`
symbols/lines:
- `getTorusQuoteForTitanX`: lines 1120–1140 — TWAP window fallback to `oldestObservation`
- `_swapTitanXForTorus`: lines 828–829 — `amountOutMinimum` derived from TWAP quote with 10% slippage
- `swapTitanXForTorusAndBuild`: lines 953–973 — incentive paid unconditionally, `_swapTitanXForTorus(half, _deadline)` called with TWAP-derived minimum

## Attacker Path
**preconditions:**
- The TORUS/TitanX Uniswap V3 pool has low observation cardinality (e.g., cardinality=1), causing `getOldestObservationSecondsAgo` to return a value much less than 900 seconds, collapsing the TWAP window to near-spot. This is a realistic condition for a pool that has not had `increaseObservationCardinalityNext` called.
- A pending `swapTitanXForTorusAndBuild` call is visible in the mempool.

**attacker-controlled inputs:**
- Front-run: sell large Torus amount for TitanX on the TORUS/TitanX pool, depressing the Torus spot price (and the near-spot TWAP).
- Back-run: buy Torus back after the victim's swap executes at the manipulated low `amountOutMinimum`.

**call sequence:**
1. Attacker front-run: `router.exactInput(TitanX→TORUS reversed, sell Torus for TitanX)` — depresses Torus price
2. Victim: `swapTitanXForTorusAndBuild(_deadline)` — `getTorusQuoteForTitanX` reads collapsed TWAP (near-spot), sets low `amountOutMinimum`, swap executes at unfavorable rate
3. Attacker back-run: buy Torus back at depressed price, capturing the spread

## Why Existing Checks Fail
- The 15-minute TWAP target is sound for a mature pool, but the fallback `if (oldestObservation < secondsAgo) secondsAgo = oldestObservation` at line 1128 is unconditional — if the pool's cardinality is 1 and the last observation is recent, `oldestObservation` can be 1–2 seconds, making the TWAP indistinguishable from spot price.
- `titanXToTorusSlippage = 10` (10%) is a wide tolerance. With a near-spot oracle, the attacker can extract up to ~10% of the swap value before the minimum output check triggers.
- `nonReentrant` and `IntervalAlreadyUsed` guards do not protect against same-block MEV sandwich.
- The 1.5% incentive is paid unconditionally after the swap, so the attacker-as-caller profits from the incentive even if the swap is sandwiched.
- No minimum `secondsAgo` floor is enforced; the code does not revert if `oldestObservation` is dangerously small (only reverts if it is exactly 0 via `consult`'s `require(secondsAgo != 0, "BP")`).

## Rejection Checks
**expected behavior checked:** The fallback to `oldestObservation` is intentional for pool bootstrapping, but it creates a window where the oracle is effectively a spot price — this is not documented as an accepted risk for the swap slippage path.

**prior report checked:** No prior report found in the repository.

**README/NatSpec checked:** No NatSpec on `getTorusQuoteForTitanX` or `_swapTitanXForTorus` documents the short-window risk as expected behavior.

**unsupported assumption checked:** The attack does not require oracle failure or malicious token owner — it only requires the pool to have low observation cardinality, which is the default state for any Uniswap V3 pool unless `increaseObservationCardinalityNext` has been called. The live context does not confirm cardinality has been increased for the TORUS/TitanX pool.

## Local Proof Required
**test type:** Foundry fork test pinned to a recent Ethereum mainnet block

**test file to add:** `src/contracts/test/SandwichBuildTest.t.sol`

**test setup:**
1. Fork mainnet at a recent block; load the deployed `TorusBuyAndProcess` contract.
2. Query the TORUS/TitanX pool's observation cardinality via `slot0()` and `observations()`.
3. If cardinality > 1, call `pool.increaseObservationCardinalityNext(1)` is not possible to reduce it — instead, test with a fresh pool deployment with cardinality=1 to simulate the vulnerable state.
4. Seed the attacker with sufficient TitanX/Torus to execute the front-run.
5. Ensure a valid build interval is available (`totalTitanXForBuild > 0`, interval not yet used).

**expected assertion:**
```
uint256 torusReceivedFair = // simulate swap without sandwich
uint256 torusReceivedSandwiched = // simulate swap with sandwich
assertGt(attackerProfit, 0, "sandwich profit > 0");
assertLt(torusReceivedSandwiched, torusReceivedFair * 90 / 100, "victim received < 90% of fair output");
```

**failure condition:** If the pool's `oldestObservation` is consistently ≥ 900 seconds (15 minutes) in practice, the TWAP manipulation cost exceeds sandwich profit and the finding does not hold for the live deployment. The test must measure actual pool cardinality and observation age to determine exploitability.

### Citations

**File:** src/contracts/src/TorusBuyAndProcess.sol (L101-101)
```text
    uint8 public titanXToTorusSlippage = 10;
```

**File:** src/contracts/src/TorusBuyAndProcess.sol (L821-840)
```text
    function _swapTitanXForTorus(
        uint256 amountTitanX,
        uint256 _deadline
    ) private returns (uint256 _torusAmount) {
        titanX.approve(UNISWAP_V3_ROUTER, amountTitanX);
        bytes memory path = abi.encodePacked(TITAN_X_ADDRESS, POOL_FEE, address(torusToken));

        uint256 expectedTorusAmount = getTorusQuoteForTitanX(amountTitanX);
        uint256 adjustedTorusAmount = (expectedTorusAmount * (100 - titanXToTorusSlippage)) / 100;

        ISwapRouter.ExactInputParams memory params = ISwapRouter
            .ExactInputParams({
                path: path,
                recipient: address(this),
                deadline: _deadline,
                amountIn: amountTitanX,
                amountOutMinimum: adjustedTorusAmount
            });
        return ISwapRouter(UNISWAP_V3_ROUTER).exactInput(params);
    }
```

**File:** src/contracts/src/TorusBuyAndProcess.sol (L942-976)
```text
    function swapTitanXForTorusAndBuild(
        uint32 _deadline
    ) external nonReentrant buildTitanXIntervalUpdate{
        if (!liquidityAddedBuild) revert NotStartedYet();

        IntervalBuild storage currInterval = titanXIntervalsBuild[
            lastTitanXBuildIntervalNumber
        ];
        if (currInterval.amountBurned != 0) revert IntervalAlreadyUsed();

        currInterval.amountBurned = currInterval.amountAllocated;
        uint256 incentive = (currInterval.amountAllocated *
            INCENTIVE_FEE_BUILD) / BPS_DENOM;

        uint256 titanXToUse = currInterval.amountAllocated - incentive;
        uint256 half = titanXToUse / 2;
        uint256 remainder = titanXToUse - half;

        totalTitanXBuilt += titanXToUse;
        totalTitanXForBuild -= titanXToUse + incentive;
        titanXUsedForBuilds += titanXToUse;

        uint256 torusPurchased = _swapTitanXForTorus(half, _deadline);

        _addLiquidityBuild(remainder, torusPurchased, _deadline);

        if (block.timestamp >= lastFractalRelease + FRACTAL_RELEASE_INTERVAL) {
            releaseFractalFunds();
        }

        burnTorus();
        TransferHelper.safeTransfer(address(titanX), msg.sender, incentive);

        emit BuyAndBuild(titanXToUse, torusPurchased, msg.sender);
    }
```

**File:** src/contracts/src/TorusBuyAndProcess.sol (L1120-1140)
```text
    function getTorusQuoteForTitanX(
        uint256 baseAmount
    ) public view returns (uint256 quote) {
        address poolAddress = torusToken.titanXTorusPool();
        uint32 secondsAgo = 15 * 60;
        uint32 oldestObservation = OracleLibrary.getOldestObservationSecondsAgo(
            poolAddress
        );
        if (oldestObservation < secondsAgo) secondsAgo = oldestObservation;
        (int24 arithmeticMeanTick, ) = OracleLibrary.consult(
            poolAddress,
            secondsAgo
        );
        uint160 sqrtPriceX96 = TickMath.getSqrtRatioAtTick(arithmeticMeanTick);
        quote = OracleLibrary.getQuoteForSqrtRatioX96(
            sqrtPriceX96,
            baseAmount,
            TITAN_X_ADDRESS,
            address(torusToken)
        );
    }
```

**File:** src/contracts/src/const/BuyAndProcessConst.sol (L5-5)
```text
uint256 constant INCENTIVE_FEE_BUILD = 150;
```
