[
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapETHForTorusAndBurn / _intervalUpdateETHForBurning] Can an unprivileged attacker call swapETHForTorusAndBurn when totalETHBurn is zero so that currInterval.amountAllocated == 0, causing the IntervalAlreadyBurned guard (currInterval.amountBurned != 0) to pass because amountBurned is written as 0 == amountAllocated, allowing the attacker to advance lastETHBurnIntervalNumber arbitrarily many times in a single block and permanently consume future interval slots with zero allocation, violating the invariant that each 10-minute interval slot is consumed exactly once with a non-zero allocation, causing scoped impact: permanent loss of all future ETH burn interval allocations held in totalETHBurn? Local proof idea: fork-test on mainnet at block 25530134 with totalETHBurn=0, call swapETHForTorusAndBurn in a loop 1000 times in one block, assert lastETHBurnIntervalNumber advanced by 1000 and all future ethIntervalsBurn slots are marked amountBurned=0 with amountAllocated=0.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: releaseFractalFunds + swapTitanXForTorusAndBurn] Can an unprivileged attacker call releaseFractalFunds() to move fractalTitanX (live: 74043750293778189300123718320 wei) into totalTitanXBurn immediately before calling swapTitanXForTorusAndBurn, so that _calculateIntervalsTitanXBurn computes amountAllocated against the inflated totalTitanXBurn, and the attacker receives INCENTIVE_FEE (150 bps) of the inflated allocation as a TitanX transfer, violating the invariant that incentive payouts are bounded by the pre-release totalTitanXBurn balance, causing scoped impact: attacker extracts up to 1.5% of 74e27 TitanX (~1.11e27 TitanX) in a single transaction? Local proof idea: fork-test, warp block.timestamp to lastFractalRelease + FRACTAL_RELEASE_INTERVAL + 1, call releaseFractalFunds() then swapTitanXForTorusAndBurn(deadline), assert attacker TitanX balance increased by ~1.5% of fractalTitanX.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: releaseFractalFunds + swapETHForTorusAndBurn] Can an unprivileged attacker call releaseFractalFunds() to move fractalETH (live: 1104000735456341638 wei) into totalETHBurn immediately before calling swapETHForTorusAndBurn, so that _calculateIntervalsETHBurn computes amountAllocated against the inflated totalETHBurn, and the attacker receives INCENTIVE_FEE (150 bps) of the inflated ETH allocation as a native ETH call{value: incentive}, violating the invariant that ETH incentive payouts are bounded by pre-release totalETHBurn, causing scoped impact: attacker extracts up to 1.5% of fractalETH (~16.56e15 wei ETH) in one transaction? Local proof idea: fork-test, warp to lastFractalRelease + 110 days + 1, call releaseFractalFunds() then swapETHForTorusAndBurn(deadline), assert attacker ETH balance delta equals incentive computed on inflated totalETHBurn.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapTitanXForTorusAndBurn / _calculateIntervalsTitanXBurn] Can an unprivileged attacker wait for N missed INTERVAL_TIME (10-minute) intervals without anyone calling swapTitanXForTorusAndBurn, then call it once to receive INCENTIVE_FEE * (amountPerInterval * (N + 1)) / BPS_DENOM TitanX in a single transaction, where amountPerInterval = totalTitanXBurn * DAILY_ALLOCATION_TITAN_X_BURNING / BPS_DENOM / INTERVALS_PER_DAY, violating the invariant that no single caller can extract more than one interval's worth of incentive per call, causing scoped impact: attacker extracts up to 1.5% of totalTitanXBurn (live: 390e24) in one call if enough intervals are missed? Local proof idea: fork-test, warp block.timestamp forward by 144 * INTERVAL_TIME (one full day), call swapTitanXForTorusAndBurn, assert TitanX transferred to attacker equals 1.5% of totalTitanXBurn.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapETHForTorusAndBurn / _calculateIntervalsETHBurn] Can an unprivileged attacker wait for N missed INTERVAL_TIME intervals without anyone calling swapETHForTorusAndBurn, then call it once to receive INCENTIVE_FEE * (amountPerInterval * (N + 1)) / BPS_DENOM ETH as native value, where amountPerInterval = totalETHBurn * DAILY_ALLOCATION_ETH_BURNING / BPS_DENOM / INTERVALS_PER_DAY, violating the invariant that no single caller can extract more than one interval's worth of ETH incentive per call, causing scoped impact: attacker extracts up to 1.5% of totalETHBurn (live: 2218081612310240917 wei) in one call? Local proof idea: fork-test, warp forward by 144 * 600 seconds, call swapETHForTorusAndBurn, assert msg.sender ETH balance increased by 1.5% of totalETHBurn.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapETHForTorusAndBurn] Can an unprivileged attacker exploit the double write of currInterval.amountBurned at lines 270 and 275 in TorusBuyAndProcess.sol, where the first write sets amountBurned = amountAllocated before the IntervalAlreadyBurned check at line 268 is evaluated, so that if amountAllocated is 0 the check (amountBurned != 0) evaluates to false and the function does not revert, allowing the attacker to call swapETHForTorusAndBurn repeatedly within the same interval without reverting, violating the invariant that each interval is processed exactly once, causing scoped impact: attacker drains gas and corrupts interval accounting? Local proof idea: unit test with totalETHBurn=0, call swapETHForTorusAndBurn twice in same block, assert second call does not revert and lastETHBurnIntervalNumber increments twice.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: burnTorus] Can an unprivileged attacker call the public burnTorus() function at any time to burn all TORUS tokens held by the TorusBuyAndProcess contract, including TORUS that was just purchased via _swapTitanXForTorus during a build flow but before _addLiquidityBuild executes, violating the invariant that TORUS purchased for liquidity addition is not burned before being added to the pool, causing scoped impact: protocol loses TORUS intended for LP, reducing liquidity depth and permanently destroying protocol-owned value? Local proof idea: fork-test, call distributeTitanXForBuilding to fund the contract, then in a separate tx call burnTorus() before swapTitanXForTorusAndBuild, assert torusToken.balanceOf(buyAndProcess) == 0 and LP position was not increased.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: burnFees] Can an unprivileged attacker call the public burnFees() function at any time to collect all accumulated LP fees from the Uniswap V3 position (tokenId from lpToken) and transfer the entire TitanX fee amount to torusToken.genesisWallet(), violating the invariant that LP fee revenue should remain under protocol control for redistribution, causing scoped impact: attacker triggers permanent transfer of all accrued TitanX LP fees to genesis wallet, bypassing any intended protocol fee distribution logic? Local proof idea: fork-test, accumulate LP fees by simulating swaps in the TitanX/TORUS pool, call burnFees() as attacker, assert TitanX balance of genesisWallet increased by full fee amount and contract TitanX balance decreased.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: addLiquidityToTorusTitanXPool] Can an unprivileged attacker call addLiquidityToTorusTitanXPool(deadline) with a manipulated TitanX/TORUS pool price (by making large trades to skew sqrtPriceX96 before the call), causing the POSITION_MANAGER.mint to use amounts at an unfavorable ratio, so that amount0 - used0 or amount1 - used1 is large and transferred to genesisWallet instead of being added to the LP, violating the invariant that all INITIAL_TITAN_X_FOR_LIQ and INITIAL_LP_MINT tokens are deposited into the pool, causing scoped impact: up to INITIAL_TITAN_X_FOR_LIQ (200e30 TitanX) or INITIAL_LP_MINT (20000e18 TORUS) is diverted to genesisWallet instead of the LP? Local proof idea: fork-test, manipulate pool price via large swap, call addLiquidityToTorusTitanXPool, assert genesisWallet received non-zero tokens and LP liquidity is less than expected.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapTitanXForTorusAndBurn / _swapTitanXForTorus / getTorusQuoteForTitanX] Can an unprivileged attacker manipulate the TitanX/TORUS Uniswap V3 TWAP oracle (15-minute window, POOL_FEE=10000) by making large trades to depress the arithmeticMeanTick, causing getTorusQuoteForTitanX to return a low expectedTorusAmount, so that adjustedTorusAmount = expectedTorusAmount * (100 - titanXToTorusSlippage) / 100 is near zero, allowing the attacker to sandwich swapTitanXForTorusAndBurn with a front-run sell and back-run buy, extracting value from the protocol's TitanX allocation, violating the invariant that swap minimum output protects protocol funds from MEV extraction, causing scoped impact: attacker extracts the difference between fair market value and manipulated minimum output from each burn interval's TitanX allocation? Local proof idea: fork-test with Foundry, manipulate TWAP over 15 minutes via repeated large swaps, then call swapTitanXForTorusAndBurn, assert torusAmount received is significantly below fair market value.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapETHForTorusAndBurn / _swapETHForTitanX / getTitanXQuoteForETH] Can an unprivileged attacker manipulate the TitanX/ETH Uniswap V3 TWAP oracle (TITAN_X_ETH_POOL: 0xc45a81bc23a64ea556ab4cdf08a86b61cdceea8b, 15-minute window) by making large trades to depress the arithmeticMeanTick, causing getTitanXQuoteForETH to return a low expectedTitanXAmount, so that adjustedTitanXAmount = expectedTitanXAmount * (100 - ethToTitanXSlippage) / 100 is near zero, allowing the attacker to sandwich _swapETHForTitanX with a front-run buy and back-run sell, extracting value from the protocol's ETH burn allocation, violating the invariant that ETH-to-TitanX swap minimum output protects protocol ETH from MEV, causing scoped impact: attacker extracts ETH value from each burn interval's ETH allocation via sandwich? Local proof idea: fork-test, manipulate TITAN_X_ETH_POOL TWAP over 15 minutes, call swapETHForTorusAndBurn, assert TitanX received is far below spot price equivalent.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapETHForTorusAndBuild / _swapETHForTitanX / _swapTitanXForTorus / _addLiquidityBuild] Can an unprivileged attacker manipulate both the TitanX/ETH and TitanX/TORUS TWAP oracles simultaneously, then call swapETHForTorusAndBuild to cause both _swapETHForTitanX and _swapTitanXForTorus to execute with near-zero minimum outputs, allowing the attacker to sandwich both swaps in sequence, extracting value from the protocol's ETH build allocation, violating the invariant that multi-hop swap minimum outputs protect protocol ETH from MEV in the build flow, causing scoped impact: attacker extracts ETH value from each build interval's ETH allocation via double sandwich? Local proof idea: fork-test, manipulate both pools' TWAPs, call swapETHForTorusAndBuild, assert torusPurchased and titanXAmount are far below fair market values.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: swapTitanXForTorusAndBuild / _addLiquidityBuild] Can an unprivileged attacker call swapTitanXForTorusAndBuild when the TitanX/TORUS pool price is manipulated so that _addLiquidityBuild uses amount0Min and amount1Min computed as (amount * (100 - liquiditySlippage)) / 100 with liquiditySlippage=10, causing POSITION_MANAGER.increaseLiquidity to accept a highly skewed ratio, and the leftover tokens (amount0 - used0 or amount1 - used1) are transferred to genesisWallet instead of remaining in the protocol, violating the invariant that all build-flow tokens are added to LP, causing scoped impact: protocol loses up to 10% of each build interval's TitanX and TORUS allocation to genesisWallet via slippage? Local proof idea: fork-test, manipulate pool price, call swapTitanXForTorusAndBuild, assert genesisWallet received non-zero TitanX or TORUS and LP liquidity increased less than expected.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: totalTorusBurnt accounting / swapTitanXForTorusAndBurn + burnTorus] Can an unprivileged attacker demonstrate that totalTorusBurnt is double-counted in swapTitanXForTorusAndBurn: line 246 adds torusAmount to totalTorusBurnt, then burnTorus() at line 253 adds torusToBurn (= torusToken.balanceOf(address(this)) = torusAmount) to totalTorusBurnt again, violating the invariant that totalTorusBurnt equals the actual cumulative TORUS burned, causing scoped impact: totalTorusBurnt is inflated by 2x per burn call, corrupting protocol accounting and any downstream logic that relies on totalTorusBurnt for economic decisions? Local proof idea: unit test, call swapTitanXForTorusAndBurn, assert totalTorusBurnt == torusAmount (not 2 * torusAmount), confirm the double-increment bug.",
  "[File: 'src/contracts/src/const/BuyAndProcessConst.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: totalTorusBurnt accounting / swapETHForTorusAndBurn + burnTorus] Can an unprivileged attacker demonstrate that totalTorusBurnt is double-counted in swapETHForTorusAndBurn: line 285 adds torusAmount to totalTorusBurnt, then burnTorus() at line 293 adds torusToBurn (= torusToken.balanceOf(address(this)) = torusAmount) to totalTorusBurnt again, violating the invariant that totalTorusBurnt == sum of all TORUS.burn() calls, causing scoped impact: totalTorusBurnt is inflated by 2x per ETH burn call, corrupting protocol accounting? Local proof idea: unit test, call swapETHForTorusAndBurn, assert totalTorusBurnt == torusAmount (not 2 * torusAmount)."
]