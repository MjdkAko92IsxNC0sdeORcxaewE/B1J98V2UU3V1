[
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapTitanXForTorusAndBurn] Can an unprivileged attacker wait for N missed 10-minute intervals (no one calls swapTitanXForTorusAndBurn) so that _calculateIntervalsTitanXBurn accumulates additionalAmount = amountPerInterval * missedIntervals up to the totalTitanXBurn cap, then call swapTitanXForTorusAndBurn to extract incentive = (totalTitanXBurn * INCENTIVE_FEE) / BPS_DENOM = 1.5% of the entire burn pool in a single call, under preconditions: liquidityAdded == true, totalTitanXBurn > 0, many intervals elapsed since lastBurnedTitanXIntervalStartTimestamp, call sequence: [wait N * INTERVAL_TIME seconds, call swapTitanXForTorusAndBurn(deadline)], violating invariant: no single unprivileged caller receives more than one interval's proportional incentive share, causing scoped impact: attacker extracts up to 1.5% of totalTitanXBurn (live value ~390047518149437255354831991687 TitanX) as TitanX reward in one tx? Local proof idea: fork test at block 25530134, warp block.timestamp by 1000 * INTERVAL_TIME, call swapTitanXForTorusAndBurn, assert TitanX received by attacker equals (min(totalTitanXBurn, amountPerInterval * (missedIntervals+1)) * 150) / 10000.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBurn] Can an unprivileged attacker wait for N missed intervals so that _calculateIntervalsETHBurn accumulates allocation up to totalETHBurn cap, then call swapETHForTorusAndBurn to extract incentive = (totalETHBurn_allocation * INCENTIVE_FEE) / BPS_DENOM ETH in a single call, under preconditions: liquidityAdded == true, totalETHBurn > 0 (live: 2218081612310240917 wei), many intervals elapsed since lastBurnedETHIntervalStartTimestamp, call sequence: [warp time, call swapETHForTorusAndBurn(deadline)], violating invariant: ETH incentive per call is bounded to one interval's proportional share, causing scoped impact: attacker receives disproportionately large ETH incentive from contract's native balance (live: 3428717047852500620 wei)? Local proof idea: fork test, warp 500 intervals, call swapETHForTorusAndBurn, assert msg.sender ETH delta equals (min(totalETHBurn, accumulated_allocation) * 150) / 10000.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapTitanXForTorusAndBurn + burnTorus] Can an unprivileged attacker observe that totalTorusBurnt is double-incremented per burn call (line 246: totalTorusBurnt += torusAmount before burnTorus() at line 253 which also does totalTorusBurnt += torusToBurn where torusToBurn == torusAmount), under preconditions: liquidityAdded == true, valid interval available, call sequence: [call swapTitanXForTorusAndBurn(deadline), read totalTorusBurnt], violating invariant: totalTorusBurnt must equal the actual cumulative Torus tokens destroyed by burn(), causing scoped impact: totalTorusBurnt accounting is inflated by 2x per burn call, corrupting any downstream reward or eligibility logic that reads this variable? Local proof idea: unit test, call swapTitanXForTorusAndBurn, assert totalTorusBurnt == torusToken.totalSupply_before - torusToken.totalSupply_after (not 2x that delta).",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBurn + burnTorus] Can an unprivileged attacker trigger the same totalTorusBurnt double-counting in swapETHForTorusAndBurn (line 286: totalTorusBurnt += torusAmount, then line 293: burnTorus() which adds torusToBurn == torusAmount again), under preconditions: liquidityAdded == true, valid ETH interval available, call sequence: [call swapETHForTorusAndBurn(deadline), read totalTorusBurnt], violating invariant: totalTorusBurnt == sum of all actual Torus.burn() call amounts, causing scoped impact: totalTorusBurnt is inflated 2x per ETH burn call, corrupting protocol-level burn accounting? Local proof idea: fork test, snapshot totalTorusBurnt before and after swapETHForTorusAndBurn, compare delta to actual Torus supply reduction.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: releaseFractalFunds + swapTitanXForTorusAndBurn] Can an unprivileged attacker call releaseFractalFunds() (public, no access control) to move fractalTitanX (live: 74043750293778189300123718320) into totalTitanXBurn just before calling swapTitanXForTorusAndBurn, inflating the interval allocation and thus the 1.5% incentive payout, under preconditions: block.timestamp >= lastFractalRelease + FRACTAL_RELEASE_INTERVAL (110 days), fractalTitanX > 0, valid interval available, call sequence: [call releaseFractalFunds(), call swapTitanXForTorusAndBurn(deadline)], violating invariant: interval allocation must be based on totalTitanXBurn at interval start, not manipulable by public state-changing calls in the same block, causing scoped impact: attacker extracts larger TitanX incentive than entitled by inflating totalTitanXBurn before allocation snapshot? Local proof idea: fork test, warp to fractal release window, call releaseFractalFunds then swapTitanXForTorusAndBurn in same block, compare incentive to baseline without releaseFractalFunds.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: releaseFractalFunds + swapETHForTorusAndBurn] Can an unprivileged attacker call releaseFractalFunds() to move fractalETH (live: 1104000735456341638 wei) into totalETHBurn just before calling swapETHForTorusAndBurn, inflating the ETH interval allocation and thus the ETH incentive, under preconditions: block.timestamp >= lastFractalRelease + FRACTAL_RELEASE_INTERVAL, fractalETH > 0, valid ETH interval available, call sequence: [call releaseFractalFunds(), call swapETHForTorusAndBurn(deadline)], violating invariant: ETH incentive per interval is bounded by the allocation computed at interval creation time, causing scoped impact: attacker receives more ETH incentive than the interval's fair share by front-running fractal release? Local proof idea: fork test, warp to fractal window, measure ETH incentive with and without prior releaseFractalFunds call.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: burnTorus] Can an unprivileged attacker call the public burnTorus() function at any time to burn all Torus tokens held by the contract and increment totalTorusBurnt, under preconditions: torusToken.balanceOf(address(this)) > 0 (e.g., after a partial swap or fee collection), call sequence: [call burnTorus()], violating invariant: Torus held by the contract for pending build liquidity operations must not be destroyable by an unprivileged caller before the build operation completes, causing scoped impact: if Torus is sent to the contract between transactions (e.g., by a third party or via fee collection), an attacker can burn it and inflate totalTorusBurnt, corrupting accounting? Local proof idea: unit test, transfer Torus to contract, call burnTorus() from attacker EOA, assert totalTorusBurnt inflated and Torus supply reduced.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: burnFees] Can an unprivileged attacker call burnFees() (no access control, nonReentrant) to collect all accumulated LP fees from the Uniswap V3 position, sending TitanX fees to genesisWallet and burning Torus fees, under preconditions: lpToken.tokenId != 0 (liquidityAdded == true), LP position has accrued fees, call sequence: [call burnFees()], violating invariant: LP fee collection and distribution should be controlled or at least not extractable by an unprivileged caller in a way that benefits the attacker, causing scoped impact: attacker can trigger fee collection at any time, potentially front-running protocol-intended fee distribution timing, and the Torus portion is burned rather than redistributed to stakers? Local proof idea: fork test, accumulate LP fees via swaps, call burnFees() from attacker, assert TitanX sent to genesisWallet and Torus burned.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: addLiquidityToTorusTitanXPool] Can an unprivileged attacker call addLiquidityToTorusTitanXPool (no onlyOwner modifier) when totalTitanXBurn >= INITIAL_TITAN_X_FOR_LIQ and liquidityAdded == false, triggering mintTokensForLP() to mint INITIAL_LP_MINT (20000e18) Torus tokens and add them to the pool at the current (potentially manipulated) pool price, under preconditions: liquidityAdded == false, totalTitanXBurn >= 200_000_000_000e18, call sequence: [manipulate pool sqrtPrice, call addLiquidityToTorusTitanXPool(deadline)], violating invariant: initial liquidity seeding must occur at a fair price and only when protocol-controlled conditions are met, causing scoped impact: attacker seeds liquidity at a manipulated price, extracting value from the minted Torus tokens or creating an unfavorable initial pool state? Local proof idea: fork test, manipulate pool price, call addLiquidityToTorusTitanXPool, assert LP position price vs fair price.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: _swapTitanXForTorus + getTorusQuoteForTitanX] Can an unprivileged attacker manipulate the TitanX/Torus Uniswap V3 TWAP oracle (when oldestObservation < 15 minutes, secondsAgo is reduced to oldestObservation) to set a low amountOutMinimum for the swap in swapTitanXForTorusAndBurn, then sandwich the swap to extract value from the protocol's TitanX burn allocation, under preconditions: pool has few observations (early stage or low cardinality), attacker controls significant liquidity, call sequence: [manipulate pool price in block N, call swapTitanXForTorusAndBurn in block N+1 with low amountOutMinimum, extract via sandwich], violating invariant: swap output must be at least the fair market value minus the configured slippage, causing scoped impact: protocol's TitanX burn allocation is swapped at an unfavorable rate, reducing Torus burned and extracting value to the sandwich attacker? Local proof idea: fork test with fresh pool, reduce observation cardinality, manipulate price, call swapTitanXForTorusAndBurn, assert torusAmount < expected fair output.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: _swapETHForTitanX + getTitanXQuoteForETH] Can an unprivileged attacker manipulate the TitanX/ETH Uniswap V3 TWAP oracle (TITAN_X_ETH_POOL at 0xc45a81bc23a64ea556ab4cdf08a86b61cdceea8b) when oldestObservation < 15 minutes to set a low adjustedTitanXAmount minimum, then sandwich swapETHForTorusAndBurn's ETH→TitanX leg, under preconditions: TWAP window is short, attacker has flash loan access to manipulate pool, call sequence: [flash loan, manipulate TITAN_X_ETH_POOL price, call swapETHForTorusAndBurn, extract via back-run], violating invariant: ETH→TitanX swap must yield at least (TWAP_quote * (100 - ethToTitanXSlippage)) / 100 TitanX, causing scoped impact: protocol's ETH burn allocation is converted to fewer TitanX tokens, reducing Torus burned and extracting value to the attacker? Local proof idea: fork test, reduce TITAN_X_ETH_POOL observation window, manipulate price, call swapETHForTorusAndBurn, assert titanXAmount < fair quote.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBurn] Can an unprivileged attacker exploit the double write to currInterval.amountBurned (line 270: currInterval.amountBurned = currInterval.amountAllocated, then line 275: currInterval.amountBurned = currInterval.amountAllocated again) to determine whether any state inconsistency window exists between the two writes that could be exploited via reentrancy or cross-function interaction, under preconditions: liquidityAdded == true, valid ETH interval, call sequence: [call swapETHForTorusAndBurn, attempt reentrancy via ETH receive callback], violating invariant: amountBurned must be set atomically and exactly once per interval, causing scoped impact: if any reentrancy path exists between the two writes, an attacker could re-enter and claim the interval twice? Local proof idea: unit test with malicious receiver contract, attempt reentrancy in receive(), assert nonReentrant guard prevents double-claim.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBuild + swapETHForTorusAndBurn] Can an unprivileged attacker exploit the shared ETH balance between totalETHBurn and totalETHForBuild (both tracked separately but drawn from the same contract ETH balance) to cause one flow to consume ETH allocated to the other, under preconditions: totalETHBurn > 0, totalETHForBuild > 0, both flows have valid intervals, call sequence: [call swapETHForTorusAndBurn to drain ETH, then call swapETHForTorusAndBuild which fails due to insufficient ETH balance], violating invariant: ETH allocated to burn flow must not be consumable by build flow and vice versa, causing scoped impact: one flow's ETH is consumed by the other, leaving the second flow unable to execute its swap? Local proof idea: fork test, drain ETH via burn flow, attempt build flow swap, assert revert or incorrect ETH accounting.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: distributeTitanXForBurning + swapTitanXForTorusAndBurn] Can an unprivileged attacker front-run a large distributeTitanXForBurning call by calling swapTitanXForTorusAndBurn in the same block before the deposit, claiming the current interval's allocation (based on pre-deposit totalTitanXBurn), then the depositor's funds inflate totalTitanXBurn for the next interval, under preconditions: valid interval available, attacker monitors mempool, call sequence: [attacker calls swapTitanXForTorusAndBurn before large deposit tx, deposit tx executes], violating invariant: interval allocation must reflect the balance at the time of interval creation, not be gameable by ordering, causing scoped impact: attacker extracts incentive from a smaller allocation than the depositor intended to fund, reducing the effective burn rate? Local proof idea: fork test, simulate front-run ordering, compare incentive extracted vs expected.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: distributeTitanXForBurning] Can an unprivileged attacker call distributeTitanXForBurning with a minimal amount (1 wei of TitanX) to trigger _intervalUpdateTitanXForBurning() when the interval condition is met, advancing the interval state without executing a burn, then immediately call swapTitanXForTorusAndBurn to claim the newly created interval's allocation, under preconditions: block.timestamp - lastBurnedTitanXIntervalStartTimestamp > INTERVAL_TIME, totalTitanXBurn > 0, call sequence: [approve 1 TitanX, call distributeTitanXForBurning(1), call swapTitanXForTorusAndBurn(deadline)], violating invariant: interval advancement must not be separable from the burn execution in a way that allows gaming the allocation snapshot, causing scoped impact: attacker controls the timing of interval creation to maximize the allocation snapshot value? Local proof idea: unit test, call distributeTitanXForBurning(1) to trigger interval update, then swapTitanXForTorusAndBurn, assert allocation matches expected.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: _calculateIntervalsTitanXBurn] Can an unprivileged attacker trigger a uint128 overflow in additionalAmount = amountPerInterval * missedIntervals (line 596, where amountPerInterval is uint128 and missedIntervals is uint32) by waiting for a very large number of missed intervals combined with a large totalTitanXBurn, causing the multiplication to overflow and revert, permanently blocking swapTitanXForTorusAndBurn, under preconditions: totalTitanXBurn is very large, many intervals missed, call sequence: [warp time by extreme amount, call swapTitanXForTorusAndBurn], violating invariant: interval calculation must not revert due to overflow for any valid combination of totalTitanXBurn and elapsed time, causing scoped impact: burn flow is permanently blocked, locking TitanX in the contract? Local proof idea: fuzz test with totalTitanXBurn near type(uint128).max and missedIntervals near type(uint32).max, assert no overflow revert.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: _calculateIntervalsETHBurn] Can an unprivileged attacker trigger a uint128 overflow in additionalAmount = amountPerInterval * missedIntervals (line 631) in the ETH burn interval calculation, blocking swapETHForTorusAndBurn, under preconditions: totalETHBurn is large, many intervals missed, call sequence: [warp time, call swapETHForTorusAndBurn], violating invariant: ETH burn interval calculation must not overflow for any valid state, causing scoped impact: ETH burn flow is permanently blocked, locking ETH in the contract? Local proof idea: fuzz test with totalETHBurn near type(uint128).max and large timeElapsed, assert no revert.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: _calculateIntervalsTitanXBurn] Can an unprivileged attacker exploit the uint128 truncation in amountPerInterval = uint128(dailyAllocation / INTERVALS_PER_DAY) (line 593-595) when dailyAllocation / INTERVALS_PER_DAY exceeds type(uint128).max, causing the interval allocation to be silently truncated to a much smaller value, under preconditions: totalTitanXBurn is extremely large (> type(uint128).max * INTERVALS_PER_DAY / DAILY_ALLOCATION_TITAN_X_BURNING), call sequence: [deposit large TitanX, call swapTitanXForTorusAndBurn], violating invariant: amountAllocated must equal the correct percentage of totalTitanXBurn without truncation, causing scoped impact: interval allocation is silently reduced, causing the burn flow to process far less TitanX than intended? Local proof idea: unit test with totalTitanXBurn = type(uint128).max * 200, assert amountAllocated matches expected without truncation.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapTitanXForTorusAndBuild + _addLiquidityBuild] Can an unprivileged attacker call swapTitanXForTorusAndBuild when the Torus/TitanX pool price is manipulated such that the liquidity addition uses very little of one token (due to price ratio), causing most of the Torus or TitanX to be sent to genesisWallet as leftover, while the attacker still receives the full 1.5% incentive on the allocated amount, under preconditions: liquidityAddedBuild == true, valid TitanX build interval, pool price is skewed, call sequence: [manipulate pool price, call swapTitanXForTorusAndBuild(deadline)], violating invariant: the build flow must add meaningful liquidity proportional to the allocated amount, causing scoped impact: protocol's build allocation is wasted (sent to genesisWallet as leftover) while attacker extracts the full incentive? Local proof idea: fork test, skew pool price, call swapTitanXForTorusAndBuild, assert used0/used1 vs amount0/amount1 ratio and incentive paid.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBuild + _addLiquidityBuild] Can an unprivileged attacker call swapETHForTorusAndBuild when the pool price is manipulated to cause maximum leftover in _addLiquidityBuild (lines 1010-1014 send leftover to genesisWallet), while still receiving the full ETH incentive, under preconditions: liquidityAddedBuild == true, valid ETH build interval, pool price skewed, call sequence: [manipulate pool price, call swapETHForTorusAndBuild(deadline)], violating invariant: ETH allocated for building must result in proportional liquidity addition, not be diverted to genesisWallet while attacker collects incentive, causing scoped impact: ETH build allocation is partially wasted while attacker extracts incentive? Local proof idea: fork test, skew pool price, call swapETHForTorusAndBuild, measure liquidity added vs ETH used.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBuild] Can an unprivileged attacker use a malicious contract as msg.sender to cause the ETH incentive transfer (line 929-933: payable(msg.sender).call{value: incentive, gas: 30000}) to fail (by consuming all 30000 gas in receive()), reverting the entire swapETHForTorusAndBuild transaction and preventing the build flow from executing, under preconditions: liquidityAddedBuild == true, valid ETH build interval, attacker deploys contract with expensive receive(), call sequence: [deploy malicious contract, call swapETHForTorusAndBuild from malicious contract], violating invariant: the build flow must complete successfully regardless of the caller's ETH receive behavior, causing scoped impact: build flow is permanently blocked for any interval where the first caller is a contract with expensive receive()? Local proof idea: unit test with malicious receiver contract, call swapETHForTorusAndBuild, assert revert.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapETHForTorusAndBurn] Can an unprivileged attacker use a malicious contract as msg.sender to cause the ETH incentive transfer (line 294-298: payable(msg.sender).call{value: incentive, gas: 30000}) to fail, reverting swapETHForTorusAndBurn and preventing the burn flow from executing for that interval, under preconditions: liquidityAdded == true, valid ETH burn interval, attacker deploys contract with expensive receive(), call sequence: [deploy malicious contract, call swapETHForTorusAndBurn from malicious contract], violating invariant: burn flow must complete regardless of caller's ETH receive behavior, causing scoped impact: ETH burn interval is permanently blocked (amountBurned already set at line 270 before the revert at line 298, but wait - nonReentrant and the revert would undo the state change)? Local proof idea: unit test with malicious receiver, call swapETHForTorusAndBurn, assert state is rolled back and interval can be retried.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: setCreateAndStakeContract] Can an unprivileged attacker call setCreateAndStakeContract (no onlyOwner modifier, line 198-209) to set torusCreateAndStake to an attacker-controlled contract if torusCreateAndStake is currently address(0), under preconditions: torusCreateAndStake == address(0) (only possible if the contract is deployed without the Torus.sol constructor calling setCreateAndStakeContract), call sequence: [deploy TorusBuyAndProcess directly, call setCreateAndStakeContract(attacker_contract)], violating invariant: torusCreateAndStake must only be set by the protocol deployer, causing scoped impact: attacker-controlled torusCreateAndStake could call distributeTitanXForBurning or distributeETHForBurning to manipulate burn pool accounting? Local proof idea: unit test, deploy TorusBuyAndProcess directly, call setCreateAndStakeContract from attacker EOA, assert it succeeds.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapTitanXForTorusAndBurn + swapETHForTorusAndBurn] Can an unprivileged attacker call both swapTitanXForTorusAndBurn and swapETHForTorusAndBurn in the same block to extract incentives from both burn flows simultaneously, under preconditions: both titanXIntervalsBurn[lastTitanXBurnIntervalNumber].amountBurned == 0 and ethIntervalsBurn[lastETHBurnIntervalNumber].amountBurned == 0, both flows have valid allocations, call sequence: [call swapTitanXForTorusAndBurn(deadline), call swapETHForTorusAndBurn(deadline) in same block], violating invariant: incentive extraction from both flows in a single block must not exceed the protocol's intended per-block reward budget, causing scoped impact: attacker extracts TitanX + ETH incentives from both flows in one block, doubling reward extraction? Local proof idea: fork test, call both functions in same block, assert combined incentive vs protocol budget.",
  "[File: 'src/contracts/src/TorusBuyAndProcess.sol -> Scope: Critical reward extraction or unfair reward access by an unprivileged attacker'] [Function: swapTitanXForTorusAndBuild + swapETHForTorusAndBuild] Can an unprivileged attacker call both swapTitanXForTorusAndBuild and swapETHForTorusAndBuild in the same block to extract incentives from both build flows simultaneously, under preconditions: both build intervals have valid allocations, call sequence: [call swapTitanXForTorusAndBuild(deadline), call swapETHForTorusAndBuild(deadline) in same block], violating invariant: combined incentive extraction per block must be bounded, causing scoped impact: attacker extracts TitanX + ETH build incentives in one block? Local proof idea: fork test, call both build functions in same block, assert combined incentive."
]