[
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.consult / TorusBuyAndProcess.getTorusQuoteForTitanX] Can an unprivileged attacker who observes that the TORUS/TITAN_X pool has a very high secondsPerLiquidityCumulativesDelta (due to very low in-range liquidity during the observation window) cause harmonicMeanLiquidity to be computed as near-zero (uint128(secondsAgoX160 / (uint192(secondsPerLiquidityCumulativesDelta) << 32)) rounds to 0), violating the invariant that harmonicMeanLiquidity must accurately reflect the pool's liquidity, causing scoped impact: any downstream logic relying on harmonicMeanLiquidity for price bounds receives a near-zero value, potentially enabling extraction? Local proof idea: fuzz test with secondsPerLiquidityCumulativesDelta near type(uint160).max, assert harmonicMeanLiquidity is 0 and check if this affects any downstream logic.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.getQuoteForSqrtRatioX96 / TorusBuyAndProcess.getTorusQuoteForTitanX] Can an unprivileged attacker who manipulates the TORUS/TITAN_X pool price to a tick where TickMath.getSqrtRatioAtTick returns type(uint160).max cause getQuoteForSqrtRatioX96 to compute ratioX192 = type(uint160).max * type(uint160).max which overflows uint256, violating the invariant that getQuoteForSqrtRatioX96 must not overflow for any valid sqrtRatioX96 value, causing scoped impact: consult() or getTorusQuoteForTitanX reverts, DoS-ing all swap functions? Local proof idea: unit test with sqrtRatioX96 = type(uint160).max, assert no overflow in ratioX192 computation (note: type(uint160).max^2 > type(uint256).max).",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.consult / TorusBuyAndProcess.getTorusQuoteForTitanX] Can an unprivileged attacker who observes that the TORUS/TITAN_X pool's tickCumulatives[1] - tickCumulatives[0] produces a value that when divided by secondsAgoInt56 yields a result outside the valid Uniswap tick range [-887272, 887272] cause the int24 cast to silently truncate the value, producing an invalid tick that TickMath.getSqrtRatioAtTick would revert on (since TickMath requires tick to be within [-887272, 887272]), violating the invariant that arithmeticMeanTick must always be a valid Uniswap tick, causing scoped impact: getTorusQuoteForTitanX reverts, DoS-ing all swap functions? Local proof idea: fuzz test with tickCumulativesDelta values that produce quotients outside [-887272, 887272] after int24 cast, assert TickMath.getSqrtRatioAtTick does not revert.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.getOldestObservationSecondsAgo / TorusBuyAndProcess.getTorusQuoteForTitanX] Can an unprivileged attacker who observes that the TORUS/TITAN_X pool has observationCardinality=1 and the single observation has blockTimestamp equal to block.timestamp (pool was initialized in the current block) cause getOldestObservationSecondsAgo to return 0, which then causes the secondsAgo cap to set secondsAgo=0, causing consult(pool, 0) to revert with 'BP', violating the invariant that the protocol must be able to execute swaps immediately after liquidity is added, causing scoped impact: the first interval after liquidityAdded=true cannot be executed, locking the first interval's allocation? Local proof idea: fork-test at the exact block where addLiquidityToTorusTitanXPool is called, call swapTitanXForTorusAndBurn in the same block, assert revert.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.consult / TorusBuyAndProcess.getTorusQuoteForTitanX / TorusBuyAndProcess.swapTitanXForTorusAndBurn] Can an unprivileged attacker who observes that the TORUS/TITAN_X pool has a very short TWAP window (secondsAgo < 60 seconds) perform a multi-block TWAP manipulation attack by gradually moving the pool price over multiple blocks, then call swapTitanXForTorusAndBurn to exploit the manipulated TWAP, violating the invariant that the 15-minute TWAP window must be used when available, causing scoped impact: attacker extracts TitanX from the burn allocation by sandwiching the swap with a manipulated oracle? Local proof idea: fork-test, simulate gradual price manipulation over multiple blocks, assert getTorusQuoteForTitanX reflects manipulated price when secondsAgo < 900.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.getQuoteForSqrtRatioX96 / TorusBuyAndProcess.getTorusQuoteForTitanX] Can an unprivileged attacker who manipulates the TORUS/TITAN_X pool price to a tick where sqrtRatioX96 is exactly type(uint128).max cause getQuoteForSqrtRatioX96 to use the ratioX192 branch (sqrtRatioX96 <= type(uint128).max), computing ratioX192 = type(uint128).max * type(uint128).max = type(uint256).max - 2*type(uint128).max + 1 which fits in uint256, then Math.mulDiv(ratioX192, baseAmount, 1<<192) which could overflow for large baseAmount, violating the invariant that getQuoteForSqrtRatioX96 must not overflow for any valid inputs, causing scoped impact: getTorusQuoteForTitanX reverts, DoS-ing all swap functions? Local proof idea: unit test with sqrtRatioX96=type(uint128).max and baseAmount=type(uint256).max, assert no overflow.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.consult / TorusCreateAndStake._distributeFees / TorusCreateAndStake.createTorus] Can an unprivileged attacker who manipulates the TITAN_X_ETH_POOL TWAP to return a very low ETH price for TitanX, then calls createTorus with ETH payment, causing _distributeFees to compute requiredGenesisETH, requiredBurnETH, requiredBuildETH, and requiredTitanXBurn all based on the manipulated requiredETH, violating the invariant that the sum of all fee components must equal the true market cost of costTitanX in ETH, causing scoped impact: attacker underpays all fee components (genesis, burn, build, titanXBurn) simultaneously, draining protocol fee revenue across all four fee buckets? Local proof idea: fork-test, manipulate TITAN_X_ETH_POOL, call createTorus with ETH, assert sum(requiredGenesisETH + requiredBurnETH + requiredBuildETH + requiredTitanXBurn) < fair_cost.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.consult / TorusCreateAndStake._distributeFees / TorusCreateAndStake.stakeTorus] Can an unprivileged attacker who manipulates the TITAN_X_ETH_POOL TWAP to return a very low ETH price for TitanX, then calls stakeTorus(torusAmount, stakingDays) with ETH payment, causing _distributeFees to compute costETH based on the manipulated oracle, violating the invariant that the staking fee must equal 5% of the fair creation cost in ETH, causing scoped impact: attacker stakes large amounts of Torus at near-zero ETH cost, gaining disproportionate staking rewards from the reward pool? Local proof idea: fork-test, manipulate TITAN_X_ETH_POOL, call stakeTorus with large torusAmount and ETH, assert costETH < expected_5pct_fee.",
  "[File: 'src/contracts/src/library/OracleLibrary.sol -> Scope: Critical fund extraction or protocol value drain by an unprivileged attacker'] [Function: OracleLibrary.consult / TorusBuyAndProcess.getTorusQuoteForTitanX] Can an unprivileged attacker who observes that the TORUS/TITAN_X pool has a very low tick (near TickMath.MIN_TICK) cause getTorusQuoteForTitanX to return a near-zero quote for any baseAmount, causing _swapTitanXForTorus to set amountOutMinimum=0, violating the invariant that amountOutMinimum must be positive for any positive amountTitanX, causing scoped impact: attacker sandwiches the TitanX→Torus swap with zero slippage protection, extracting the full TitanX allocation? Local proof idea: unit test with arithmeticMeanTick = TickMath.MIN_TICK, assert getTorusQuoteForTitanX(1e18) > 0."
]